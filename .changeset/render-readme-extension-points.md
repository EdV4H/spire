---
"@edv4h/spire-render": patch
---

README に 0.2.0 の拡張点を書いた。カスタムノード/エッジ/レイヤレンダラ、図形ボキャブラリ（なぜ JSX ではなく図形を返すのか）、`scale`、差し替え可能な描画バックエンドと `keyboardAccessible` の代償。コードの変更はない — README は tarball に入るので、npm のページを直すには publish が要る。
